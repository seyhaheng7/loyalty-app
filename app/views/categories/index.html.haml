= render 'partials/page-head', title: 'Listing Category'
= link_to new_category_path, class: 'btn btn-primary' do
  = fa_icon 'plus'
  Add New Category
= datagrid_form_for @grid, :method => :get, :url => users_path
= paginate(@grid.assets)
= datagrid_table @grid
= paginate(@grid.assets)
