= render 'partials/page-head', title: 'Listing Company'
= link_to new_company_path, class: 'btn btn-primary' do
  = fa_icon 'plus'
  Add New Company
= datagrid_form_for @grid, :method => :get, :url => users_path
= paginate(@grid.assets)
= datagrid_table @grid
= paginate(@grid.assets)
