= render 'partials/page-head', title: 'Listing FAQs'

.m-heading-1.border-green.m-bordered
  = datagrid_form_for @grid, :method => :get, :url => faqs_path

.portlet.light.bordered
  .portlet-title
    .caption
      %i.icon-wrench
      FAQs
    .pull-right
      -if policy(@grid.assets).new?
        = link_to new_faq_path, class: 'btn btn-primary' do
          = fa_icon 'plus'
          Add New FAQ
  .portlet-body
    .table-scrollable
      = datagrid_table @grid
    .text-center
      = paginate(@grid.assets)