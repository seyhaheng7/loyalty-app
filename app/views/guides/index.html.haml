.m-heading-1.border-green.m-bordered
  = datagrid_form_for @grid, :method => :get, :url => guides_path

.portlet.light.bordered
  .portlet-title
    .caption
      %i.icon-pie-chart
      Listing Guides
    .pull-right
      -if policy(@grid.assets).new?
        = link_to new_guide_path, class: 'btn btn-primary' do
          = fa_icon 'plus'
          Add New Guide
  .portlet-body
    .table-scrollable
      = datagrid_table @grid
    .text-center
      = paginate(@grid.assets)