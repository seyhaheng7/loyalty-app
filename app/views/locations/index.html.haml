= render 'partials/page-head', title: 'Listing Location'
= link_to new_location_path, class: 'btn btn-primary' do
  = fa_icon 'plus'
  Add New Location

= datagrid_form_for @grid, :method => :get, :url => users_path
= paginate(@grid.assets)
= datagrid_table @grid
= paginate(@grid.assets)
