= render 'partials/page-head', title: 'Listing Merchant'

.m-heading-1.border-green.m-bordered
  = datagrid_form_for @grid, :method => :get, :url => merchants_path

.portlet.light.bordered
  .portlet-title
    .caption
      %i.icon-user-following
      Merchant
    .pull-right
      -if policy(@grid.assets).new?
        = link_to new_merchant_path, class: 'btn btn-primary' do
          = fa_icon 'plus'
          Add New Merchant
  .portlet-body
    .table-scrollable
      = datagrid_table @grid
    .text-center
      = paginate(@grid.assets)