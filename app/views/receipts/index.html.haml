= render 'partials/page-head', title: 'Listing Receipt'
= link_to new_receipt_path, class: 'btn btn-primary' do
  = fa_icon 'plus'
  Add New Receipt
= datagrid_form_for @grid, :method => :get, :url => users_path
= paginate(@grid.assets)
= datagrid_table @grid
= paginate(@grid.assets)
