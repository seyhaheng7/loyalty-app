= render 'partials/page-head', title: 'Listing StickerGroups'

%br

.m-heading-1.border-green.m-bordered
  = datagrid_form_for @grid, :method => :get, :url => sticker_groups_path

.portlet.light.bordered
  .portlet-title
    .caption
      %i.icon-basket
      StickerGroup
  .portlet-body
    -if policy(@grid.assets).new?
      = link_to new_sticker_group_path, class: 'btn btn-primary' do
        = fa_icon 'plus'
        Add New StickerGroup
    .table-scrollable
      = paginate(@grid.assets)
      = datagrid_table @grid
      = paginate(@grid.assets)








