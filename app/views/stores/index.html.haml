= render 'partials/page-head', title: 'Listing Stores'

.m-heading-1.border-green.m-bordered
  = datagrid_form_for @grid, :method => :get, :url => stores_path

.portlet.light.bordered
  .portlet-title
    .caption
      %i.icon-basket
      Stores
    .pull-right
      -if policy(@grid.assets).new?
        = link_to new_store_path, class: 'btn btn-primary' do
          = fa_icon 'plus'
          Add New Store
  .portlet-body
    .table-scrollable
      = datagrid_table @grid
    .text-center
      = paginate(@grid.assets)